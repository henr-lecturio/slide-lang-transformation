# Input video (keeps original file inside /data)
VIDEO_PATH="data/Code of Conduct Basics Lecturio.mp4"

# Phase name as expected by SliTraNet folder layout.
PHASE="test"

# Slide ROI in full-frame pixel coordinates (x0,y0,x1,y1).
# Adjust this first if detections are unstable.
ROI_X0=20
ROI_Y0=20
ROI_X1=1320
ROI_Y1=820
